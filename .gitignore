# Captured HAR files contain session-related headers (e.g. x-csrf-token)
# and third-party personal data. Never commit them.
*.har

# macOS
.DS_Store

# Python
__pycache__/
*.pyc

# GitHub repo analyzer: fetched repo cache (metadata + shallow clones)
_repo_cache/

# NOTE: reports/ is intentionally tracked (public analysis reports).

# Local notes / memoranda (backlog, drafts, etc.) stay out of git.
docs/
